https://wiki.archlinux.org/title/Installation_guide_(Русский)
https://habr.com/ru/articles/819729/

## основные команды:

Чтобы **выбрать раскладку**, передайте её название команде loadkeys. Например, чтобы выбрать русскую раскладку, выполните команду:

```bash
 loadkeys ru
```
для переключения моду раскладками нужно использовать **`Ctrl+Shift`**

Для корректного отображения кириллицы нужно выбрать подходящий консольный шрифт. Консольные шрифты расположены в каталоге `/usr/share/kbd/consolefonts/` и могут быть выбраны аналогичным способом с помощью команды setfont без указания полного пути и расширения.

Например, для загрузки шрифта стандартного размера с русскими буквами выполните:

```bash
setfont cyr-sun16
```

Для экрана HiDPI можно выбрать один из самых больших доступных шрифтов с русскими буквами:

```bash
setfont ter-c32b
```
### проверка режима загрузки, проверьте разрядность UEFI:
```bash
 cat /sys/firmware/efi/fw_platform_size
```
 в идеале должно вернуть 64 но с 32 тоже будет работать 
 
### Соединение с интернетом
Для настройки сетевого соединения в live-окружении выполните следующие действия:

Убедитесь, что ваш сетевой интерфейс обнаружен и включён, например, с помощью ip-link:
```bash
ip link
```
Если планируется использовать Wi-Fi или WWAN, убедитесь, что беспроводная карта не заблокирована с помощью rfkill.
Подключитесь к сети:
Ethernet: подключите кабель.

**Wi-Fi**: подключитесь к беспроводной сети с помощью `iwctl`.
разблокируем:
```bash
rfkill unblock wifi
```

запуск утилиты для работы с сетью
```bash
iwctl
```

просмотр сетевых карт/модемов
```bash
device list
```

*предположим доступен **wlan0***

просмотр доступных Wi-Fi сeтей

```bash
station wlan0 scan
```

вывод списка доступных сетей
```bash
station wlan0 get-networks
```

соединимся с желаемой сетью
```
station wlan0 connect <Network name сети из списка выше>
```

и выход
```bash
exit
```

Мобильный широкополосный модем: подключитесь к мобильной сети с помощью утилиты `mmcli`.
Настройте сетевые соединения:
DHCP: установка динамического IP-адреса и адреса DNS-сервера (при помощи `systemd-networkd` и `systemd-resolved`) должна срабатывать автоматически для проводных и беспроводных сетевых интерфейсов.
Статический IP: смотрите Настройка сети#Статический IP-адрес.
Соединение можно проверить с помощью утилиты ping:

```bash
ping ping.archlinux.org
```

### Разметка дисков
Когда запущенная система распознает накопители, они становятся доступны как блочные устройства, например, `/dev/sda`, `/dev/nvme0n1` или `/dev/mmcblk0`. Чтобы посмотреть их список, используйте lsblk или fdisk.

```bash
fdisk -l
```

Результаты, оканчивающиеся на rom, loop и airootfs, можно игнорировать. Устройства mmcblk*, оканчивающиеся на rpmb, boot0 и boot1, тоже можно игнорировать.

**На выбранном накопителе должны присутствовать следующие разделы:**

Раздел для корневого каталога `/`.
Для загрузки в режиме UEFI также необходим системный раздел EFI.
Для изменения таблицы разделов используйте инструмент разметки, например fdisk:
```bash
 fdisk /dev/диск_для_разметки
```

|Точка монтирования в установленной системе|Раздел|[Тип раздела](https://en.wikipedia.org/wiki/ru:%D0%A2%D0%B0%D0%B1%D0%BB%D0%B8%D1%86%D0%B0_%D1%80%D0%B0%D0%B7%D0%B4%D0%B5%D0%BB%D0%BE%D0%B2_GUID#.D0.98.D0.B4.D0.B5.D0.BD.D1.82.D0.B8.D1.84.D0.B8.D0.BA.D0.B0.D1.82.D0.BE.D1.80.D1.8B_.28GUID.29_.D1.80.D0.B0.D0.B7.D0.BB.D0.B8.D1.87.D0.BD.D1.8B.D1.85_.D1.82.D0.B8.D0.BF.D0.BE.D0.B2_.D1.80.D0.B0.D0.B7.D0.B4.D0.B5.D0.BB.D0.BE.D0.B2 "wikipedia:ru:Таблица разделов GUID")|Рекомендуемый размер|
|---|---|---|---|
|`/boot`1|`/dev/_системный_раздел_efi_`|[Системный раздел EFI](https://wiki.archlinux.org/title/%D0%A1%D0%B8%D1%81%D1%82%D0%B5%D0%BC%D0%BD%D1%8B%D0%B9_%D1%80%D0%B0%D0%B7%D0%B4%D0%B5%D0%BB_EFI "Системный раздел EFI")|1 ГБ|
|`[SWAP]`|`/dev/_раздел_подкачки_`|Linux swap|Не менее 4 ГБ|
|`/`|`/dev/_корневой_раздел_`|Linux x86-64 root (/)|Остаток, минимум 23–32 ГБ|


### Форматирование разделов
После создания разделов их нужно отформатировать в подходящую файловую систему.
Например, чтобы отформатировать `/dev/корневой_раздел` в файловую систему Ext4, выполните следующую команду:

```bash
mkfs.ext4 /dev/корневой_раздел
```
**Если вы создали раздел для подкачки (swap), инициализируйте его с помощью утилиты mkswap:**

```bash
mkswap /dev/раздел_подкачки
```

**Примечание**
> В случае с многоуровневыми (stacked) блочными устройствами замените `/dev/*_раздел` соответствующим путём к блочному устройству.

Если вы создали системный раздел EFI, отформатируйте его в FAT32 с помощью mkfs.fat.

## Важно
>**Выполняйте форматирование, только если вы создали новый раздел в процессе разметки. Если системный раздел EFI уже существует, его форматирование уничтожит загрузчики других установленных операционных систем.**

```bash
mkfs.fat -F 32 /dev/системный_раздел_efi
```

## Монтирование разделов
Смонтируйте корневой раздел в каталог /mnt. Например, если корневой раздел — `/dev/корневой_раздел`, выполните следующую команду:

```bash
mount --mkdir /dev/корневой_раздел /mnt
```

Создайте точки монтирования в каталоге `/mnt` для всех остальных разделов (например, если точка монтирования раздела в установленной системе будет `/boot`, то создайте каталог `/mnt/boot`) и примонтируйте соответствующие разделы.

> аргумент `--mkdir` автоматически создает точку монтирования

***Для UEFI примонтируйте системный раздел EFI. Например:***

```bash
mount --mkdir /dev/системный_раздел_efi /mnt/boot
```

Если вы ранее создали раздел подкачки, активируйте его с помощью `swapon`:

```bash
swapon /dev/раздел_подкачки
```

В дальнейшем `genfstab` обнаружит смонтированные файловые системы и пространство подкачки.

# установка

### Установка основных пакетов

Пакеты для установки будут скачиваться с серверов-зеркал, прописанных в файле `/etc/pacman.d/mirrorlist`. Чем выше зеркало расположено в списке, тем больший приоритет оно имеет при скачивании пакета.

Никакие программы или настройки (кроме файла `/etc/pacman.d/mirrorlist`) из live-окружения в устанавливаемую систему не переносятся.
Единственным обязательным для установки пакетом является base, который не предоставляет все инструменты, имеющиеся на установочном носителе, поэтому вам скорее всего понадобится установить дополнительные пакеты для получения полностью функциональной базовой системы.

**Пример команды для базовой установки с ядром Linux и прошивками для часто встречающихся устройств:**

```bash
pacstrap -K /mnt base linux linux-firmware
```


## настройка 

### Fstab (авто монтирывание) 

Чтобы необходимые файловые системы (вроде той, которая используется для каталога `/boot`) автоматически монтировались при загрузке системы, сгенерируйте файл [fstab](https://wiki.archlinux.org/title/Fstab_\(%D0%A0%D1%83%D1%81%D1%81%D0%BA%D0%B8%D0%B9\) "Fstab (Русский)"). Используйте ключ `-U` или `-L`, чтобы для идентификации разделов использовались [UUID](https://wiki.archlinux.org/title/Persistent_block_device_naming_\(%D0%A0%D1%83%D1%81%D1%81%D0%BA%D0%B8%D0%B9\)#by-uuid "Persistent block device naming (Русский)") или метки соответственно:

```bash
genfstab -U /mnt >> /mnt/etc/fstab
```

### Chroot 

Чтобы напрямую взаимодействовать с новой системой, её инструментами и настройками для выполнения следующих шагов, как если бы вы загрузились в неё, перейдите к корневому каталогу новой системы:

```bash
arch-chroot /mnt
```

### Время

Для удобства пользователей (например, для отображения правильного местного времени или учёта летнего времени) задайте [часовой пояс](https://wiki.archlinux.org/title/%D0%A7%D0%B0%D1%81%D0%BE%D0%B2%D0%BE%D0%B9_%D0%BF%D0%BE%D1%8F%D1%81 "Часовой пояс"):

```bash
ln -sf /usr/share/zoneinfo/_Район_/_Расположение_ /etc/localtime
```

Запустите [hwclock](https://man.archlinux.org/man/hwclock.8), чтобы сгенерировать `/etc/adjtime`:

```bash
hwclock --systohc
```

Эта команда предполагает, что аппаратные часы используют [UTC](https://en.wikipedia.org/wiki/ru:%D0%92%D1%81%D0%B5%D0%BC%D0%B8%D1%80%D0%BD%D0%BE%D0%B5_%D0%BA%D0%BE%D0%BE%D1%80%D0%B4%D0%B8%D0%BD%D0%B8%D1%80%D0%BE%D0%B2%D0%B0%D0%BD%D0%BD%D0%BE%D0%B5_%D0%B2%D1%80%D0%B5%D0%BC%D1%8F "wikipedia:ru:Всемирное координированное время"). Для получения дополнительной информации смотрите раздел [System time (Русский)#Стандарт времени](https://wiki.archlinux.org/title/System_time_\(%D0%A0%D1%83%D1%81%D1%81%D0%BA%D0%B8%D0%B9\)#Стандарт_времени "System time (Русский)").

Чтобы предотвратить дрейф часов и обеспечить точность времени, настройте его [синхронизацию](https://wiki.archlinux.org/title/System_time_\(%D0%A0%D1%83%D1%81%D1%81%D0%BA%D0%B8%D0%B9\)#Синхронизация_времени "System time (Русский)") с помощью клиента [Network Time Protocol](https://en.wikipedia.org/wiki/ru:NTP "wikipedia:ru:NTP") (NTP), например [systemd-timesyncd](https://wiki.archlinux.org/title/Systemd-timesyncd_\(%D0%A0%D1%83%D1%81%D1%81%D0%BA%D0%B8%D0%B9\) "Systemd-timesyncd (Русский)").

### Локализация

Чтобы использовать правильное форматирование для конкретного региона и языка (например, в датах, валютах и числах), [отредактируйте](https://wiki.archlinux.org/title/Help:Reading_\(%D0%A0%D1%83%D1%81%D1%81%D0%BA%D0%B8%D0%B9\)#Добавить,_создать,_редактировать "Help:Reading (Русский)") файл `/etc/locale.gen`, раскомментировав `en_US.UTF-8 UTF-8` и другие UTF-8 [локали](https://wiki.archlinux.org/title/%D0%9B%D0%BE%D0%BA%D0%B0%D0%BB%D0%B8 "Локали"), которые вы будете использовать (например, `ru_RU.UTF-8 UTF-8`), после чего сгенерируйте их:

```bash
locale-gen
```

[Создайте](https://wiki.archlinux.org/title/%D0%A1%D0%BE%D0%B7%D0%B4%D0%B0%D0%B9%D1%82%D0%B5 "Создайте") файл [locale.conf](https://man.archlinux.org/man/locale.conf.5) и [задайте переменной LANG](https://wiki.archlinux.org/title/Locale_\(%D0%A0%D1%83%D1%81%D1%81%D0%BA%D0%B8%D0%B9\)#Установка_системной_локали "Locale (Русский)") необходимое значение:

```
/etc/locale.conf

LANG=_ru_RU.UTF-8_
```

Если вы [меняли раскладку клавиатуры или шрифт](https://wiki.archlinux.org/title/Installation_guide_\(%D0%A0%D1%83%D1%81%D1%81%D0%BA%D0%B8%D0%B9\)#Установка_раскладки_клавиатуры_и_шрифта), сделайте эти изменения постоянными, прописав их в файле [vconsole.conf](https://man.archlinux.org/man/vconsole.conf.5):

```
/etc/vconsole.conf

KEYMAP=_ru_
FONT=_cyr-sun16_
```

### Настройка сети

Чтобы присвоить вашей системе постоянное легко узнаваемое имя (особенно полезно при взаимодействии через сеть), [создайте](https://wiki.archlinux.org/title/%D0%A1%D0%BE%D0%B7%D0%B4%D0%B0%D0%B9%D1%82%D0%B5 "Создайте") файл [hostname](https://wiki.archlinux.org/title/Network_configuration_\(%D0%A0%D1%83%D1%81%D1%81%D0%BA%D0%B8%D0%B9\)#Имя_хоста "Network configuration (Русский)"):

```
/etc/hostname

_имявашегохоста_
```

имя хоста должно содержать от 1 до 63 символов, допустимы только строчные латинские буквы от `a` до `z`, цифры от `0` до `9` и дефис `-`, при этом `-` не должен быть первым символом.

### Пароль суперпользователя

Установите безопасный [пароль](https://wiki.archlinux.org/title/Users_and_groups_\(%D0%A0%D1%83%D1%81%D1%81%D0%BA%D0%B8%D0%B9\)#База_данных_пользователей "Users and groups (Русский)") для [суперпользователя](https://en.wikipedia.org/wiki/ru:root "wikipedia:ru:root") — аккаунта, через который будет выполняться администрирование системы:

```bash
passwd
```

### Загрузчик

Выберите загрузчик, подходящий для созданной вами таблицы разделов, и установите его. Ознакомьтесь с пояснениями и сравнительной таблицей в разделе [Процесс загрузки Arch#Загрузчик](https://wiki.archlinux.org/title/%D0%9F%D1%80%D0%BE%D1%86%D0%B5%D1%81%D1%81_%D0%B7%D0%B0%D0%B3%D1%80%D1%83%D0%B7%D0%BA%D0%B8_Arch#Загрузчик "Процесс загрузки Arch") и следуйте инструкциям по установке, приведённым в статье выбранного вами загрузчика.

#### пример с **GRUB2**
### Установка загрузчика

- Для установки GRUB2 нужно выполнить команду от root:

```bash
grub-install /dev/sda
```

где `/dev/sda` это устройство _(не раздел!)_ для установки загрузочных образов GRUB. Файлы загрузчика будут установлены в каталог `/boot`. Код GRUB (`boot.img`) будет встроен в начальный сектор, а загрузочный образ `core.img` – в просвет перед первым разделом [MBR](https://wiki.archlinux.org/title/MBR_\(%D0%A0%D1%83%D1%81%D1%81%D0%BA%D0%B8%D0%B9\) "MBR (Русский)"), или BIOS boot partition для [GPT](https://wiki.archlinux.org/title/GPT_\(%D0%A0%D1%83%D1%81%D1%81%D0%BA%D0%B8%D0%B9\) "GPT (Русский)").

- Если при выполнении команды происходит ошибка, попробуйте добавить **`--recheck`** в аргументы как показано ниже:

```bash
grub-install --recheck /dev/sda
```

- Если требуется установить файлы загрузчика в другой каталог, его можно указать в опции **--boot-directory** . С этой опцией можно легко установить GRUB2 на диск с другой системой _(устанавливаемой или исправляемой)_ **без чрута**, достаточно правильно указать текущий путь к смонтированному каталогу для установки и текущее имя устройства, примерно так:

```bash
grub-install --boot-directory=/mnt/boot /dev/sdb
```

- Если нужно установить BIOS-версию загрузчика из-под системы, загруженной в режиме [UEFI](https://wiki.archlinux.org/title/UEFI_\(%D0%A0%D1%83%D1%81%D1%81%D0%BA%D0%B8%D0%B9\) "UEFI (Русский)"), требуется принудительно задать программе установки нужную сборку GRUB:

```bash
grub-install --target=i386-pc /dev/sda
```

### Перезагрузка

Выйдите из окружения chroot, набрав `exit` или нажав `Ctrl+d`.

Вы можете размонтировать все разделы с помощью команды `umount -R /mnt`, чтобы убедиться в том, что ни один из разделов не остался занят какой-либо программой. Если нужно, для поиска таких программ используйте [fuser](https://man.archlinux.org/man/fuser.1).

Теперь перезагрузите компьютер, набрав `reboot`: если какие-нибудь разделы остались смонтированными, [systemd](https://wiki.archlinux.org/title/Systemd_\(%D0%A0%D1%83%D1%81%D1%81%D0%BA%D0%B8%D0%B9\) "Systemd (Русский)") их размонтирует. Не забудьте извлечь установочный носитель. После загрузки войдите в систему в качестве суперпользователя (root).